# SAJHA MCP Server v3.1.0 — Dependencies
# Copyright All rights Reserved 2025-2030, Ashutosh Sinha
#
# Install: pip install -r requirements.txt

# ── Core Framework ───────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
jinja2>=3.1.0

# ── Database ─────────────────────────────────────────────────
sqlalchemy>=2.0.25

# ── PostgreSQL Driver (needed when db.type=postgresql) ───────
psycopg2-binary>=2.9.9
# Alternative: psycopg[binary]>=3.1.0

# ── MySQL Driver (needed for MCP Studio DB Query tools) ──────
pymysql>=1.1.0

# ── Authentication ───────────────────────────────────────────
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# ── MCP Transport (SSE) ─────────────────────────────────────
sse-starlette>=1.8.0

# ── HTTP Clients ─────────────────────────────────────────────
httpx>=0.26.0
requests>=2.31.0

# ── Schema, Config & Utilities ───────────────────────────────
jsonschema>=4.19.0
python-dotenv>=1.0.0
pyyaml>=6.0

# ── Analytics ────────────────────────────────────────────────
duckdb>=1.0.0

# ── Financial Data: Yahoo Finance (35 yfinance_* tools) ──────
yfinance>=0.2.36

# ── Financial Data: OpenBB Platform (70 openbb_* tools) ──────
# Core SDK + data providers installed via extras.
# Install all providers:
#   pip install "openbb[all]"
# Or pick specific ones:
openbb[yfinance,fmp,fred,sec,ecb,imf,oecd,worldbank,alpha_vantage,benzinga,intrinio,polygon,tiingo,tradingeconomics,nasdaq]>=4.3.0

# ── Testing ──────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0

# ── AI Integration: LLM Provider SDKs ────────────────────────
anthropic>=0.39.0                   # Claude API (official SDK)
openai>=1.50.0                      # OpenAI GPT + embeddings (official SDK)
together>=1.3.0                     # Together.ai open-source models
ollama>=0.4.0                       # Ollama local models
# boto3 already listed above for AWS/S3 — also covers Bedrock

# ── v4.0.0: Observability (optional — falls back to built-in metrics) ─
# opentelemetry-api>=1.20.0
# opentelemetry-sdk>=1.20.0
# opentelemetry-exporter-otlp>=1.20.0
# Uncomment above for OTEL trace/metric export to Datadog/Grafana/Splunk
